import React from 'react';
import styled from 'styled-components';

export const NotFound: React.FC = () => {
  return <Wrapper>Page Not Found.Please check access path.</Wrapper>;
};

const Wrapper = styled.div``;
